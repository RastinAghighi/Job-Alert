/target/
**/*.rs.bkl

# AWS SAM build artefacts (Phase 6)
.aws-sam/

# Secrets must live in SSM Parameter Store (§27), never in the repo.
.env
*.pem
credentials

# Local-only project documents; never tracked or uploaded.
/documents/

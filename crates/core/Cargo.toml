[package]
name         = "jobmon-core"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
publish.workspace      = true

[lints]
workspace = true

[dependencies]
jobmon-errors.workspace = true

# `sha2` + `data-encoding` implement §13.2.2 (SHA-256, RFC 4648 standard Base32,
# unpadded). `chrono` carries no `clock` feature — see the workspace manifest.
chrono.workspace        = true
data-encoding.workspace = true
serde.workspace         = true
serde_json.workspace    = true
sha2.workspace          = true
